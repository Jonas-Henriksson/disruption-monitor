export const V2_CSS = `
@keyframes v2spin { from { transform: rotate(0deg) } to { transform: rotate(360deg) } }
@keyframes v2pulse { 0%, 100% { opacity: 1 } 50% { opacity: 0.5 } }
@keyframes v2fadeIn { from { opacity: 0; transform: translateY(-4px) } to { opacity: 1; transform: translateY(0) } }
@keyframes v2slideDown { from { opacity: 0; max-height: 0 } to { opacity: 1; max-height: 500px } }
`;
